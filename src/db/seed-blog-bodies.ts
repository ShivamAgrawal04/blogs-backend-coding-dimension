/** Blog HTML bodies used only by db seed scripts. Runtime serves content from the database. */

export const awsEc2UbuntuDeployment = "<h2>Overview</h2>\n<p>Deploy a <strong>Node.js / Express</strong> backend on an <strong>AWS EC2 Ubuntu</strong> instance using <strong>PM2</strong> and <strong>Nginx</strong> as a reverse proxy. Follow the sections in order the first time; later use the command reference when something breaks.</p>\n\n<h2>1. Connect to EC2</h2>\n<pre><code>ssh -i your-key.pem ubuntu@YOUR_SERVER_IP</code></pre>\n<p>Example:</p>\n<pre><code>ssh -i myserver.pem ubuntu@13.126.xxx.xxx</code></pre>\n<ul>\n<li>Exit server: <code>exit</code></li>\n<li>Switch to root (optional): <code>sudo su</code></li>\n<li>Current user: <code>whoami</code></li>\n<li>Current directory: <code>pwd</code></li>\n</ul>\n\n<h2>2. Update Ubuntu</h2>\n<pre><code>sudo apt update\nsudo apt upgrade -y\nsudo apt autoremove -y\nsudo apt clean</code></pre>\n\n<h2>3. Install Git</h2>\n<pre><code>sudo apt install git -y\ngit --version\n\ngit config --global user.name \"Your Name\"\ngit config --global user.email \"your-email@example.com\"\ngit config --list\nwhich git</code></pre>\n\n<h2>4. Install Node.js (LTS)</h2>\n<pre><code>sudo apt install curl -y\ncurl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -\nsudo apt install nodejs -y\n\nnode -v\nnpm -v\nwhich node\nwhich npm</code></pre>\n\n<h2>5. Clone the repository</h2>\n<pre><code># HTTPS\ngit clone https://github.com/username/project.git\n\n# SSH\ngit clone git@github.com:username/project.git\n\ncd project\ngit branch\ngit pull origin main</code></pre>\n\n<h2>6. Environment variables</h2>\n<pre><code>nano .env</code></pre>\n<p>Example <code>.env</code>:</p>\n<pre><code>PORT=5000\nDATABASE_URL=\nJWT_SECRET=\nREDIS_URL=\nAWS_ACCESS_KEY=\nAWS_SECRET_KEY=</code></pre>\n<p>In nano: <code>Ctrl+O</code>, Enter, <code>Ctrl+X</code>. Verify with <code>cat .env</code> and <code>ls -la</code>.</p>\n\n<h2>7. Install dependencies</h2>\n<pre><code>npm install\n\n# Clean install if needed\nrm -rf node_modules package-lock.json\nnpm install</code></pre>\n\n<h2>8. Build the project</h2>\n<pre><code># TypeScript\nnpm run build\n\n# Prisma (if used)\nnpx prisma generate\nnpx prisma migrate deploy\nnpm run seed</code></pre>\n\n<h2>9. Test the app</h2>\n<pre><code>npm run dev      # development\nnpm start        # production (often node dist/server.js)</code></pre>\n\n<h2>9.1 Install Redis (optional)</h2>\n<pre><code>sudo apt install redis-server -y</code></pre>\n\n<h2>9.2 Install MongoDB (optional)</h2>\n<h3>Server packages</h3>\n<pre><code>curl -fsSL https://pgp.mongodb.com/server-8.0.asc | \\\\\n  sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor\n\necho \"deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] https://repo.mongodb.org/apt/ubuntu noble/mongodb-org/8.0 multiverse\" | \\\\\n  sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list\n\nsudo apt update\nsudo apt install -y mongodb-org\nmongod --version</code></pre>\n<h3>Start and enable</h3>\n<pre><code>sudo systemctl start mongod\nsudo systemctl enable mongod\nsudo systemctl status mongod</code></pre>\n<p>Expected: <code>Active: active (running)</code>.</p>\n<h3>Kernel compatibility override (if needed)</h3>\n<pre><code>sudo systemctl edit mongod</code></pre>\n<p>Add:</p>\n<pre><code>[Service]\nEnvironment=\"MONGODB_DISABLE_KERNEL_VERSION_CHECK=true\"</code></pre>\n<pre><code>sudo systemctl daemon-reload\nsudo systemctl restart mongod\nsudo systemctl status mongod</code></pre>\n<h3>Logs and shell</h3>\n<pre><code>sudo journalctl -u mongod -n 50 --no-pager\nsudo tail -50 /var/log/mongodb/mongod.log\n\nsudo apt install mongodb-mongosh\nmongosh</code></pre>\n<p>MongoDB Compass is a <strong>GUI</strong> — it needs a desktop display. On a headless EC2 box, use <code>mongosh</code> instead of Compass.</p>\n\n<h2>10. Install and run with PM2</h2>\n<pre><code>sudo npm install -g pm2\npm2 -v\n\npm2 start npm --name backend -- start\n# or\npm2 start dist/server.js --name backend</code></pre>\n\n<h2>11. Essential PM2 commands</h2>\n<pre><code>pm2 list\npm2 logs\npm2 logs backend\npm2 restart backend\npm2 reload backend\npm2 stop backend\npm2 delete backend\npm2 monit\npm2 save\npm2 startup\n# run the command PM2 prints, then:\npm2 save</code></pre>\n\n<h2>12. Install Nginx</h2>\n<pre><code>sudo apt install nginx -y\nsudo systemctl status nginx\nsudo systemctl start nginx\nsudo systemctl enable nginx\nsudo systemctl restart nginx\nsudo systemctl reload nginx</code></pre>\n\n<h2>13. Nginx reverse proxy</h2>\n<pre><code>sudo nano /etc/nginx/sites-available/backend</code></pre>\n<pre><code>server {\n    listen 80;\n    server_name YOUR_SERVER_IP;\n\n    location / {\n        proxy_pass http://localhost:5000;\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection 'upgrade';\n        proxy_set_header Host $host;\n        proxy_cache_bypass $http_upgrade;\n    }\n}</code></pre>\n<pre><code>sudo ln -s /etc/nginx/sites-available/backend /etc/nginx/sites-enabled/\nsudo nginx -t\nsudo systemctl restart nginx</code></pre>\n\n<h2>14. Firewall (UFW)</h2>\n<pre><code>sudo apt install ufw -y\nsudo ufw allow OpenSSH\nsudo ufw allow 80\nsudo ufw allow 443\nsudo ufw enable\nsudo ufw status</code></pre>\n\n<h2>15. Useful Ubuntu commands</h2>\n<pre><code>pwd\nls\nls -la\ncd foldername\ncd ..\ncd ~\ncd /\nmkdir folder\nrm -rf folder\nrm file.txt\nmv old new\ncp file file2</code></pre>\n\n<h2>16. Disk, memory, and CPU</h2>\n<pre><code>df -h\ndu -sh *\nfree -h\nlscpu</code></pre>\n\n<h2>17. Processes and ports</h2>\n<pre><code>ps aux\nps aux | grep node\nkill PID\nkill -9 PID\n\nsudo ss -tulpn\nsudo lsof -i :5000\nsudo kill -9 PID</code></pre>\n\n<h2>18. Logs</h2>\n<pre><code>pm2 logs\nsudo tail -f /var/log/nginx/access.log\nsudo tail -f /var/log/nginx/error.log\njournalctl -xe</code></pre>\n\n<h2>19. Git on the server</h2>\n<pre><code>git status\ngit pull\ngit fetch\ngit branch\ngit checkout main\ngit log --oneline</code></pre>\n\n<h2>20. Node / npm helpers</h2>\n<pre><code>node -v\nnpm -v\nnpm list -g --depth=0\nnpm cache clean --force</code></pre>\n\n<h2>21. Service status</h2>\n<pre><code>systemctl status nginx\nsystemctl status ssh\nsystemctl status docker\nsudo systemctl restart nginx</code></pre>\n\n<h2>22. Common errors and fixes</h2>\n<h3>Port already in use</h3>\n<pre><code>sudo lsof -i :5000\nsudo kill -9 PID</code></pre>\n<h3>Node not found</h3>\n<pre><code>which node\nnode -v\nsudo apt install nodejs -y</code></pre>\n<h3>Git permission denied</h3>\n<pre><code>ssh-keygen -t ed25519 -C \"your@email.com\"\ncat ~/.ssh/id_ed25519.pub</code></pre>\n<p>Add the public key to GitHub, then clone over SSH again.</p>\n<h3>PM2 not found</h3>\n<pre><code>sudo npm install -g pm2</code></pre>\n<h3>Nginx config test</h3>\n<pre><code>sudo nginx -t</code></pre>\n<h3>Reboot / shutdown</h3>\n<pre><code>sudo reboot\nsudo shutdown now</code></pre>\n\n<h2>Deployment checklist</h2>\n<ul>\n<li>Ubuntu updated</li>\n<li>Git + Node installed</li>\n<li>Repo cloned</li>\n<li><code>.env</code> created</li>\n<li><code>npm install</code> + build done</li>\n<li>PM2 running and saved (<code>pm2 startup</code> + <code>pm2 save</code>)</li>\n<li>Nginx installed and reverse proxy configured</li>\n<li>Firewall allows SSH + 80/443</li>\n<li>Backend reachable</li>\n<li>Domain + SSL (recommended next step)</li>\n</ul>\n\n<h2>Final URLs</h2>\n<ul>\n<li>Without Nginx: <code>http://SERVER_IP:5000</code></li>\n<li>With Nginx: <code>http://SERVER_IP</code></li>\n<li>With SSL: <code>https://yourdomain.com</code></li>\n</ul>\n<p>Once PM2 and Nginx are stable, every deploy is usually: <code>git pull</code> → build → <code>pm2 restart backend</code>.</p>\n";

export const deployNextjsCloudflare = "<h2>Why Cloudflare instead of Vercel</h2>\n<p>Vercel is the obvious default for Next.js — it is built by the same team. But if you are already using Cloudflare for DNS, or you want a generous free tier for a side project, <strong>Workers</strong> is a real alternative.</p>\n<p>The catch: Next.js was not built for Cloudflare’s runtime natively, so you need a translation layer called <strong>OpenNext</strong> to turn a standard Next.js build into something Workers can run.</p>\n<p>This guide covers a real App Router setup on Cloudflare Workers — including two mistakes that each cost a full debugging session.</p>\n\n<h2>Installing OpenNext for Cloudflare</h2>\n<p>Two packages get you started:</p>\n<pre><code>npm install --save-dev @opennextjs/cloudflare wrangler --legacy-peer-deps</code></pre>\n<ul>\n<li><code>wrangler</code> — Cloudflare’s CLI for deploying Workers</li>\n<li><code>@opennextjs/cloudflare</code> — adapter that converts your <code>.next</code> build into a Worker</li>\n</ul>\n\n<h3>open-next.config.ts</h3>\n<pre><code>import { defineCloudflareConfig } from \"@opennextjs/cloudflare\";\n\nexport default defineCloudflareConfig();</code></pre>\n\n<h3>wrangler.jsonc</h3>\n<pre><code>{\n  \"$schema\": \"node_modules/wrangler/config-schema.json\",\n  \"name\": \"your-app-name\",\n  \"main\": \".open-next/worker.js\",\n  \"compatibility_date\": \"2026-06-01\",\n  \"compatibility_flags\": [\"nodejs_compat\", \"global_fetch_strictly_public\"],\n  \"assets\": {\n    \"directory\": \".open-next/assets\",\n    \"binding\": \"ASSETS\"\n  },\n  \"observability\": {\n    \"enabled\": true\n  }\n}</code></pre>\n<p>The <code>nodejs_compat</code> flag matters more than it looks — without it, any dependency using Node built-ins like <code>crypto</code> or <code>buffer</code> fails at <strong>runtime</strong>, not build time.</p>\n\n<h2>Build scripts</h2>\n<pre><code>{\n  \"scripts\": {\n    \"dev\": \"next dev\",\n    \"build\": \"next build\",\n    \"cf:build\": \"opennextjs-cloudflare build\",\n    \"cf:preview\": \"opennextjs-cloudflare build && opennextjs-cloudflare preview\",\n    \"cf:deploy\": \"opennextjs-cloudflare build && opennextjs-cloudflare deploy\"\n  }\n}</code></pre>\n<p><code>cf:preview</code> is the step people skip — and it is exactly what would have caught the middleware bug below before production.</p>\n\n<h2>Gotcha #1: middleware.ts vs proxy.ts</h2>\n<p>On Next.js 16, the framework nudges you toward <code>proxy.ts</code> instead of <code>middleware.ts</code> for request interception.</p>\n<p>Problem: <code>proxy.ts</code> runs on the <strong>Node.js</strong> runtime. Cloudflare Workers via OpenNext only supports the <strong>Edge</strong> runtime for middleware. Rename to <code>proxy.ts</code>, deploy, and production silently stops running your middleware — no error, no warning. Auth redirects look like a Clerk/cookie bug.</p>\n<p><strong>Fix:</strong> keep the file named <code>middleware.ts</code>.</p>\n<pre><code>import { clerkMiddleware, createRouteMatcher } from \"@clerk/nextjs/server\";\nimport { NextResponse } from \"next/server\";\n\nconst isDashboard = createRouteMatcher([\"/dashboard(.*)\"]);\nconst isPublicOnly = createRouteMatcher([\"/\", \"/sign-in(.*)\", \"/sign-up(.*)\"]);\n\nexport default clerkMiddleware(async (auth, req) => {\n  const { userId } = await auth();\n\n  if (userId && isPublicOnly(req)) {\n    return NextResponse.redirect(new URL(\"/dashboard\", req.url));\n  }\n\n  if (!userId && isDashboard(req)) {\n    await auth.protect();\n  }\n});\n\n// middleware.ts (not proxy.ts): Cloudflare Workers via OpenNext only supports\n// the Edge middleware runtime, and Next 16's proxy.ts is Node-only.\nexport const config = {\n  matcher: [\n    \"/((?!_next|[^?]*\\\\\\\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)\",\n    \"/(api|trpc)(.*)\",\n  ],\n};</code></pre>\n<p>If you deploy Next.js 16 to Cloudflare: ignore the <code>proxy.ts</code> migration guide for Workers and stay on <code>middleware.ts</code>.</p>\n\n<h2>Test with the real runtime before deploying</h2>\n<pre><code>npm run cf:build\nnpm run cf:preview</code></pre>\n<p><code>next dev</code> runs on Node no matter what your production target is. <code>cf:preview</code> runs the built Worker through <strong>workerd</strong> — Cloudflare’s real runtime — on your machine.</p>\n\n<h2>Environment variables: two buckets</h2>\n<ul>\n<li><strong>Variables</strong> — plain text. Use for <code>NEXT_PUBLIC_*</code> values (baked into the client bundle at build time).</li>\n<li><strong>Secrets</strong> — encrypted. Use for Clerk secret keys, deploy keys, payment keys.</li>\n</ul>\n<pre><code>npx wrangler secret put CLERK_SECRET_KEY</code></pre>\n<p><code>NEXT_PUBLIC_*</code> values must exist at <strong>build time</strong>, not only runtime. With Cloudflare Git integration, set them in that build’s environment screen — it is separate from Worker runtime variables.</p>\n\n<h2>Wiring up GitHub auto-deploy</h2>\n<ol>\n<li>Cloudflare dashboard → Workers &amp; Pages → Connect to Git</li>\n<li>Authorize GitHub and pick the repo</li>\n<li>Build command: <code>npm run cf:build</code></li>\n<li>Add every <code>NEXT_PUBLIC_*</code> value and secret in the <strong>build</strong> environment</li>\n</ol>\n<p>After that, every push to your production branch builds and deploys automatically.</p>\n\n<h2>Gotcha #2: peer-dependency error only on Cloudflare</h2>\n<p>A GitHub-connected build can fail at install with a peer range error for <code>@opennextjs/cloudflare</code>, even when the same Next version installs fine locally. Cloudflare’s npm resolves multi-clause <code>||</code> peer ranges more strictly.</p>\n<p>Add a root <code>.npmrc</code>:</p>\n<pre><code>legacy-peer-deps=true</code></pre>\n<p>Commit it, push, and the next auto-build gets past install cleanly.</p>\n\n<h2>Pitfalls worth remembering</h2>\n<ul>\n<li><strong>middleware.ts, not proxy.ts</strong> — Next.js 16 rename is Node-only and silently no-ops on Workers</li>\n<li><strong>nodejs_compat</strong> — miss it and Node-dependent packages break at runtime</li>\n<li><strong>NEXT_PUBLIC_*</strong> must be in the build environment or the client bundle gets <code>undefined</code></li>\n<li><strong>.npmrc with legacy-peer-deps=true</strong> — avoids Cloudflare install peer conflicts</li>\n<li><strong>Always run cf:preview</strong> before deploying — only local step that uses Cloudflare’s runtime</li>\n</ul>\n<p>None of this is documented clearly in one place, which is why these bugs are easy to ship. Get the middleware file name and <code>.npmrc</code> right from the start.</p>\n";

export const gcpSshOsLoginWindows = "<h2>Overview</h2>\n<p>Connect to a <strong>Google Cloud Compute Engine</strong> Linux VM from <strong>Windows</strong> using <strong>OS Login</strong>. This guide covers SSH key creation, Google Cloud CLI install, adding your public key, enabling OS Login on the VM, IAM roles, and connecting — including the common <strong>project number vs project ID</strong> mistake.</p>\n<p>Follow the sections in order the first time. Later use the cheat sheet when something breaks.</p>\n\n<h2>What you need</h2>\n<ul>\n<li>A GCP project (example: <code>testing-purpose-503607</code>)</li>\n<li>A Linux VM (example: <code>instance-20260811-081515</code> in zone <code>asia-south2-a</code>)</li>\n<li>The Google account you use with <code>gcloud</code></li>\n<li>Windows Command Prompt and PowerShell (Admin)</li>\n</ul>\n\n<h2>1. Create an SSH key pair</h2>\n<p>Open <strong>Command Prompt</strong> and run <code>ssh-keygen</code> with <code>-C</code> for the username comment:</p>\n<pre><code>ssh-keygen -t rsa -f C:\\Users\\WINDOWS_USER\\.ssh\\KEY_FILENAME -C USERNAME</code></pre>\n<p>Or with ed25519 (recommended) and your profile path:</p>\n<pre><code>ssh-keygen -t ed25519 -f %USERPROFILE%\\.ssh\\KEY_FILENAME -C USERNAME</code></pre>\n<p>Replace:</p>\n<ul>\n<li><code>WINDOWS_USER</code> — your Windows username (or use <code>%USERPROFILE%</code>)</li>\n<li><code>KEY_FILENAME</code> — key file name (example: <code>gcMachine</code> or <code>id_ed25519</code>). This creates <code>KEY_FILENAME</code> (private) and <code>KEY_FILENAME.pub</code> (public)</li>\n<li><code>USERNAME</code> — VM username comment. For OS Login this is often like <code>calcbase_official_gmail_com</code></li>\n</ul>\n<p>Files are saved under:</p>\n<ul>\n<li>Private: <code>C:\\Users\\WINDOWS_USER\\.ssh\\KEY_FILENAME</code></li>\n<li>Public: <code>C:\\Users\\WINDOWS_USER\\.ssh\\KEY_FILENAME.pub</code></li>\n</ul>\n<p>A public key looks like:</p>\n<pre><code>ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQDAu5kKQCPF... cloudysanfrancisco</code></pre>\n<p>or:</p>\n<pre><code>ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... your-comment</code></pre>\n<p><strong>Never share the private key.</strong> Only upload the <code>.pub</code> file.</p>\n<p>Notes:</p>\n<ul>\n<li>On Linux VMs, do not use <code>root</code> unless you explicitly allow root login</li>\n<li>For Windows AD VMs, use <code>DOMAIN\\username</code> (this guide focuses on Linux + OS Login)</li>\n</ul>\n\n<h2>2. Install the Google Cloud CLI</h2>\n<p>Open <strong>PowerShell as Administrator</strong> and run:</p>\n<pre><code>(New-Object Net.WebClient).DownloadFile(\n  \"https://dl.google.com/dl/cloudsdk/channels/rapid/GoogleCloudSDKInstaller.exe\",\n  \"$env:Temp\\GoogleCloudSDKInstaller.exe\"\n)\n&amp; \"$env:Temp\\GoogleCloudSDKInstaller.exe\"</code></pre>\n<p>In the installer:</p>\n<ul>\n<li>Accept defaults (Next → Next → Install)</li>\n<li>The installer is signed by Google LLC</li>\n<li>If you use a screen reader, enable <strong>Turn on screen reader mode</strong></li>\n<li>When finished, <strong>untick all options</strong> (Start Menu / Desktop shortcuts and “start the Google Cloud CLI shell”). You will configure <code>gcloud</code> next yourself</li>\n</ul>\n<p>Open a new Command Prompt and initialize:</p>\n<pre><code>gcloud init</code></pre>\n<p>Sign in and select your project.</p>\n\n<h2>3. List projects (use PROJECT ID, not number)</h2>\n<pre><code>gcloud projects list</code></pre>\n<p>Example output:</p>\n<pre><code>PROJECT_ID: testing-purpose-503607\nNAME: Testing purpose\nPROJECT_NUMBER: 88909271654</code></pre>\n<p>OS Login commands need <strong>PROJECT_ID</strong> (<code>testing-purpose-503607</code>), not the project number.</p>\n\n<h2>4. Add your public key with OS Login</h2>\n<pre><code>gcloud compute os-login ssh-keys add ^\n  --key-file=KEY_FILE_PATH ^\n  --project=PROJECT ^\n  --ttl=EXPIRE_TIME</code></pre>\n<p>Real example:</p>\n<pre><code>gcloud compute os-login ssh-keys add --key-file=\"%USERPROFILE%\\.ssh\\id_ed25519.pub\" --project=testing-purpose-503607</code></pre>\n\n<h3>Mistake: project number instead of project ID</h3>\n<pre><code>gcloud compute os-login ssh-keys add --key-file=\"%USERPROFILE%\\.ssh\\id_ed25519.pub\" --project=88909271654</code></pre>\n<p>Error:</p>\n<pre><code>ERROR: (gcloud.compute.os-login.ssh-keys.add) The value of `--project` flag was set to Project number.\nTo use this command, set it to PROJECT ID instead.</code></pre>\n<p>Fix: use <code>--project=testing-purpose-503607</code>.</p>\n\n<h3>Successful response</h3>\n<p>On success you get a login profile. Important fields:</p>\n<ul>\n<li><code>username</code> — SSH as this user (example: <code>calcbase_official_gmail_com</code>)</li>\n<li><code>homeDirectory</code> — example: <code>/home/calcbase_official_gmail_com</code></li>\n<li><code>sshPublicKeys</code> — confirms your public key is registered</li>\n</ul>\n<p>Copy the <code>username</code>. You need it for plain <code>ssh</code>.</p>\n\n<h2>5. Enable OS Login on the VM</h2>\n<p>Recommended: enable OS Login only on this instance.</p>\n<pre><code>gcloud compute instances add-metadata instance-20260811-081515 --zone=asia-south2-a --metadata=enable-oslogin=TRUE</code></pre>\n<p>Replace the instance name and zone with yours if different.</p>\n\n<h2>6. Grant OS Login IAM roles</h2>\n<p>Basic SSH (no sudo):</p>\n<pre><code>gcloud projects add-iam-policy-binding testing-purpose-503607 --member=\"user:YOUR_GOOGLE_EMAIL\" --role=\"roles/compute.osLogin\"</code></pre>\n<p>SSH + sudo / admin on the VM:</p>\n<pre><code>gcloud projects add-iam-policy-binding testing-purpose-503607 --member=\"user:YOUR_GOOGLE_EMAIL\" --role=\"roles/compute.osAdminLogin\"</code></pre>\n<p>Replace <code>YOUR_GOOGLE_EMAIL</code> with the Google account used for <code>gcloud</code>.</p>\n<p>Google requires <code>roles/compute.osLogin</code> or <code>roles/compute.osAdminLogin</code> for OS Login SSH access. If you already own the project, you may already have enough access — add the role explicitly if SSH still fails with a permission error.</p>\n\n<h2>7. Connect to the VM</h2>\n\n<h3>Option A — preferred: gcloud SSH</h3>\n<p><code>gcloud compute ssh</code> handles OS Login for you:</p>\n<pre><code>gcloud compute ssh instance-20260811-081515 --zone=asia-south2-a</code></pre>\n\n<h3>Option B — plain ssh</h3>\n<pre><code>ssh -i \"%USERPROFILE%\\.ssh\\gcMachine\" calcbase_official_gmail_com@34.126.216.66</code></pre>\n<p>Use:</p>\n<ul>\n<li>The private key that matches the <code>.pub</code> you registered</li>\n<li>The OS Login <code>username</code> from step 4</li>\n<li>Your VM external IP</li>\n</ul>\n\n<h2>Quick checklist</h2>\n<ol>\n<li>SSH key exists under <code>%USERPROFILE%\\.ssh\\</code></li>\n<li>Google Cloud CLI installed and <code>gcloud init</code> done</li>\n<li>Public key added with <code>os-login ssh-keys add</code> using <strong>project ID</strong></li>\n<li>VM metadata has <code>enable-oslogin=TRUE</code></li>\n<li>IAM role <code>osLogin</code> or <code>osAdminLogin</code> granted</li>\n<li>Connect with <code>gcloud compute ssh</code> or <code>ssh -i ... username@ip</code></li>\n</ol>\n\n<h2>Command cheat sheet</h2>\n<pre><code>REM List projects\ngcloud projects list\n\nREM Add public key (OS Login) — use PROJECT ID\ngcloud compute os-login ssh-keys add --key-file=%USERPROFILE%\\.ssh\\id_ed25519.pub --project=PROJECT_ID\n\nREM Enable OS Login on one VM\ngcloud compute instances add-metadata INSTANCE_NAME --zone=ZONE --metadata=enable-oslogin=TRUE\n\nREM Grant roles\ngcloud projects add-iam-policy-binding PROJECT_ID --member=\"user:EMAIL\" --role=\"roles/compute.osLogin\"\ngcloud projects add-iam-policy-binding PROJECT_ID --member=\"user:EMAIL\" --role=\"roles/compute.osAdminLogin\"\n\nREM Connect\ngcloud compute ssh INSTANCE_NAME --zone=ZONE\nssh -i %USERPROFILE%\\.ssh\\KEY_FILENAME OS_LOGIN_USERNAME@EXTERNAL_IP</code></pre>\n\n<h2>What’s next</h2>\n<p>Once you are inside the VM, continue with deploy work: update packages, install Docker / Node / pnpm, clone your repo, run Postgres, configure <code>.env</code>, and start backend + frontend (optionally behind Nginx).</p>\n";

export const pushOneRepoTwoGithubAccounts = "<h2>Overview</h2>\n<p>One local Git project can update <strong>two GitHub accounts</strong> at once. You keep a single working folder and add a second remote — no re-clone, no copy-paste of files.</p>\n<p>This guide assumes <strong>Repo A is already live on GitHub</strong> and you want the same history on <strong>Repo B under a second account</strong>. It covers HTTPS remotes first, then the Windows <strong>SSH multi-account</strong> setup that usually bites people next.</p>\n\n<pre><code>                    ┌── GitHub Account 1 (origin)\nLocal Project ──────┤\n                    └── GitHub Account 2 (second)</code></pre>\n\n<h2>What you need</h2>\n<ul>\n<li>An existing local Git repo that already pushes to Account 1</li>\n<li>A second GitHub account with permission to create repositories</li>\n<li>On Windows: Git for Windows + OpenSSH (built into modern Windows)</li>\n<li>Optional but recommended for 2 accounts: a separate SSH key per GitHub account</li>\n</ul>\n\n<h2>1. Stay in your existing project</h2>\n<p>Do <strong>not</strong> delete or re-clone. Open the folder that already works:</p>\n<pre><code>cd \"D:\\path\\to\\your\\project\"\ngit status</code></pre>\n<p>You want something like:</p>\n<pre><code>On branch main\nYour branch is up to date with 'origin/main'</code></pre>\n<p>If Git says you are not on a branch or the folder is not a repo, fix that first — this guide starts from a healthy local repo.</p>\n<p>Confirm your current branch name:</p>\n<pre><code>git branch --show-current</code></pre>\n<p>If you still use <code>master</code>, either push <code>master</code> below or rename once:</p>\n<pre><code>git branch -M main</code></pre>\n\n<h2>2. Leave the first remote alone</h2>\n<pre><code>git remote -v</code></pre>\n<p>Typical output:</p>\n<pre><code>origin  https://github.com/OLD-ACCOUNT/my-project.git (fetch)\norigin  https://github.com/OLD-ACCOUNT/my-project.git (push)</code></pre>\n<p>Or with SSH:</p>\n<pre><code>origin  git@github.com:OLD-ACCOUNT/my-project.git (fetch)\norigin  git@github.com:OLD-ACCOUNT/my-project.git (push)</code></pre>\n<p><strong>Do not change <code>origin</code>.</strong> That remote is your already-working Account 1 connection.</p>\n\n<h2>3. Create an empty repo on Account 2</h2>\n<ol>\n<li>Log into the <strong>second</strong> GitHub account</li>\n<li>Click <strong>New repository</strong></li>\n<li>Name it (example: <code>my-project</code>)</li>\n<li>Keep it empty: no README, no <code>.gitignore</code>, no license</li>\n<li>Create the repository</li>\n</ol>\n<p>Copy the URL GitHub shows. HTTPS example:</p>\n<pre><code>https://github.com/SECOND-ACCOUNT/my-project.git</code></pre>\n<p>SSH example (we will refine this for multi-account SSH later):</p>\n<pre><code>git@github.com:SECOND-ACCOUNT/my-project.git</code></pre>\n<p>Use the <strong>exact</strong> owner username from the Code → SSH / HTTPS button. Typos in the username are the #1 cause of <code>Repository not found</code> after SSH auth succeeds.</p>\n\n<h2>4. Add a second remote</h2>\n<pre><code>git remote add second https://github.com/SECOND-ACCOUNT/my-project.git</code></pre>\n<p>Verify:</p>\n<pre><code>git remote -v</code></pre>\n<pre><code>origin   https://github.com/OLD-ACCOUNT/my-project.git\nsecond   https://github.com/SECOND-ACCOUNT/my-project.git</code></pre>\n<p>You can name the remote anything (<code>backup</code>, <code>work</code>, <code>personal</code>). This guide uses <code>second</code>.</p>\n\n<h2>5. Push to Account 2</h2>\n<pre><code>git push -u second main</code></pre>\n<p>If your branch is still <code>master</code>:</p>\n<pre><code>git push -u second master</code></pre>\n<p>Daily workflow after that:</p>\n<pre><code>git push origin main    # Account 1\ngit push second main    # Account 2</code></pre>\n\n<h2>6. HTTPS authentication tip</h2>\n<p>GitHub no longer accepts account passwords for Git over HTTPS. Use a <strong>Personal Access Token (PAT)</strong> when prompted for a password.</p>\n<p>If Windows Credential Manager already saved Account 1, Account 2 HTTPS pushes can silently use the wrong credentials. For two accounts long-term, prefer <strong>SSH with Host aliases</strong> (next sections).</p>\n\n<h2>7. Why two GitHub accounts need SSH aliases</h2>\n<p>GitHub sees every SSH connection as <code>git@github.com</code>. If both accounts use the default key, GitHub always authenticates as whichever key is offered first — usually Account 1. Account 2 then looks “broken” even though the remote URL looks fine.</p>\n<p>Fix: give each account its own key and a custom <code>Host</code> alias in <code>~/.ssh/config</code>.</p>\n\n<h3>Create a second key (if you do not have one)</h3>\n<pre><code>ssh-keygen -t ed25519 -C \"second-account@email.com\" -f %USERPROFILE%\\.ssh\\id_ed25519_second</code></pre>\n<p>Add the <code>.pub</code> file to GitHub → Account 2 → Settings → SSH and GPG keys.</p>\n\n<h3>Windows SSH config</h3>\n<p>Edit (create if missing):</p>\n<pre><code>notepad %USERPROFILE%\\.ssh\\config</code></pre>\n<p>Example with two accounts:</p>\n<pre><code>Host github.com\n    HostName github.com\n    User git\n    IdentityFile C:/Users/YOUR_WINDOWS_USER/.ssh/id_ed25519\n    IdentitiesOnly yes\n\nHost github-second\n    HostName github.com\n    User git\n    IdentityFile C:/Users/YOUR_WINDOWS_USER/.ssh/id_ed25519_second\n    IdentitiesOnly yes</code></pre>\n<p><code>IdentitiesOnly yes</code> forces that Host to use only the listed key — critical when multiple keys exist.</p>\n<p>The <code>Host</code> name on the left (<code>github-second</code>) is an alias. Your Git remote must use <strong>that exact alias</strong>, not a name you invent later.</p>\n\n<h2>8. Point <code>second</code> at the SSH alias</h2>\n<pre><code>git remote set-url second git@github-second:SECOND-ACCOUNT/my-project.git</code></pre>\n<p>Test the alias (not plain <code>github.com</code>):</p>\n<pre><code>ssh -T git@github-second</code></pre>\n<p>Success looks like:</p>\n<pre><code>Hi SECOND-ACCOUNT! You've successfully authenticated, but GitHub does not provide shell access.</code></pre>\n<p>Then push:</p>\n<pre><code>git push -u second main</code></pre>\n\n<h2>Common mistakes (and exact fixes)</h2>\n\n<h3>A. <code>ssh: Could not resolve hostname github-wisdora</code></h3>\n<p>You used an SSH alias in the remote URL that does <strong>not</strong> exist in <code>~/.ssh/config</code>.</p>\n<p>Check what aliases you actually have:</p>\n<pre><code>type %USERPROFILE%\\.ssh\\config</code></pre>\n<p>If your config says:</p>\n<pre><code>Host My_id_second\n    HostName github.com\n    User git\n    IdentityFile C:/Users/YOUR_WINDOWS_USER/.ssh/My_id_second_github\n    IdentitiesOnly yes</code></pre>\n<p>then the remote must use <code>My_id_second</code>, not <code>github-second</code> or <code>github-wisdora</code>:</p>\n<pre><code>git remote set-url second git@My_id_second:SECOND-ACCOUNT/my-project.git\nssh -T git@My_id_second</code></pre>\n\n<h3>B. SSH says Hi username… but push says Repository not found</h3>\n<p>Authentication worked. The repo path is wrong or you lack access.</p>\n<p>Classic typo pattern:</p>\n<ul>\n<li>SSH authenticated as <code>shivamagrawals</code></li>\n<li>Remote pointed at <code>shivamagarwals/...</code> (extra <code>a</code>)</li>\n</ul>\n<p>GitHub returns <code>ERROR: Repository not found</code> for both missing repos and private repos you cannot see — so double-check the owner spelling.</p>\n<p>Fix:</p>\n<ol>\n<li>Open the second-account repo in the browser</li>\n<li>Click <strong>Code → SSH</strong> and copy the exact URL</li>\n<li>Replace only the host with your alias</li>\n</ol>\n<p>If Code → SSH shows:</p>\n<pre><code>git@github.com:correct-username/my-project.git</code></pre>\n<p>set:</p>\n<pre><code>git remote set-url second git@My_id_second:correct-username/my-project.git\ngit remote -v\ngit push -u second main</code></pre>\n\n<h3>C. Wrong account authenticated</h3>\n<p><code>ssh -T git@github-second</code> greets Account 1 instead of Account 2. Usually missing <code>IdentitiesOnly yes</code>, or the wrong <code>IdentityFile</code> path. Fix the config block, then retest with <code>ssh -T</code> before pushing.</p>\n\n<h3>D. Second remote already exists</h3>\n<pre><code>error: remote second already exists.</code></pre>\n<p>Update the URL instead of adding again:</p>\n<pre><code>git remote set-url second git@My_id_second:SECOND-ACCOUNT/my-project.git</code></pre>\n\n<h2>Optional: push both remotes in one command</h2>\n<p>You can add a second push URL on <code>origin</code>:</p>\n<pre><code>git remote set-url --add --push origin https://github.com/OLD-ACCOUNT/my-project.git\ngit remote set-url --add --push origin https://github.com/SECOND-ACCOUNT/my-project.git\ngit push origin main</code></pre>\n<p>That updates both destinations when you push <code>origin</code>. Many people still prefer two named remotes (<code>origin</code> + <code>second</code>) so each push target stays explicit and easier to debug.</p>\n\n<h2>Cheat sheet</h2>\n<pre><code># Inspect\ngit status\ngit branch --show-current\ngit remote -v\ntype %USERPROFILE%\\.ssh\\config\nssh -T git@YOUR_SSH_ALIAS\n\n# Add / fix second remote\ngit remote add second https://github.com/SECOND-ACCOUNT/my-project.git\ngit remote set-url second git@YOUR_SSH_ALIAS:SECOND-ACCOUNT/my-project.git\n\n# Push\ngit push origin main\ngit push -u second main</code></pre>\n\n<h2>Final mental model</h2>\n<ul>\n<li><strong>One local folder</strong> — one Git history</li>\n<li><strong>Two remotes</strong> — two GitHub homes</li>\n<li><strong>Two SSH Host aliases</strong> — two GitHub identities on one PC</li>\n<li>If <code>ssh -T</code> greets the right user, auth is done; remaining errors are almost always the <code>owner/repo</code> path</li>\n</ul>\n<p>Paste <code>git remote -v</code> and the greeting from <code>ssh -T git@YOUR_ALIAS</code> whenever something fails — those two lines usually pinpoint the next fix.</p>\n";
